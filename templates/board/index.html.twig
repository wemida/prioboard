{% extends 'base.html.twig' %}

{% block title %}PrioBoard{% endblock %}
{% block body_class %}board-screen{% endblock %}
{% block body_attributes %}
    data-board-version="{{ settings.boardVersion }}"
    data-refresh-interval="{{ settings.refreshInterval }}"
    data-meta-url="{{ path('api_board_meta') }}"
{% endblock %}

{% block body %}
    <main class="board-page">
        <header class="board-header">
            <div>
                <h1>PrioBoard</h1>
            </div>
            <nav class="admin-nav">
                <a class="button" href="{{ path('app_login') }}">Edit</a>
            </nav>
        </header>

        <section class="board-grid">
            {% for key, label in columns %}
                <article class="board-column board-column-{{ key }}">
                    <header class="column-header">
                        <h2>{{ label }}</h2>
                        <span>{{ cardsByColumn[key]|length }}</span>
                    </header>

                    <div class="column-cards">
                        {% for card in cardsByColumn[key] %}
                            <div class="board-card color-{{ card.color ?? 'neutral' }}">
                                <p><span class="card-index">{{ loop.index }}.</span> {{ card.title }}</p>
                            </div>
                        {% else %}
                            <div class="board-card board-card-empty">
                                <p>No items</p>
                            </div>
                        {% endfor %}
                    </div>
                </article>
            {% endfor %}
        </section>
    </main>
{% endblock %}
